---
import type { Locale } from '../../i18n/routes';
import { getRoute } from '../../i18n/routes';
import {
  getTranslations,
  getVisibleTranslations
} from '../../i18n/translations';
import Logo from '../ui/Logo.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = getTranslations(locale);
const visible = getVisibleTranslations(locale);
const year = new Date().getFullYear();
const linkedInLabel =
  locale === 'pl' ? 'ClearStance na LinkedIn' : 'ClearStance on LinkedIn';
---

<footer class="site-footer">
  <div class="wrap footer-primary">
    <a class="brand-link" href={getRoute(locale, 'home')} aria-label={t.site.homeLabel}>
      <Logo dark />
    </a>
    <p>{visible.site.footerTagline}</p>
    <nav aria-label={locale === 'pl' ? 'Nawigacja w stopce' : 'Footer navigation'}>
      <a href={getRoute(locale, 'services')}>{locale === 'pl' ? 'Doradztwo' : 'Advisory'}</a>
      <a href={getRoute(locale, 'exercises')}>{locale === 'pl' ? 'Ćwiczenia' : 'Exercises'}</a>
      <a href={getRoute(locale, 'insights')}>{visible.site.nav.insights}</a>
      <a href={getRoute(locale, 'about')}>{visible.site.nav.experience}</a>
      <a href={getRoute(locale, 'contact')}>{visible.site.nav.contact}</a>
    </nav>
  </div>
  <div class="wrap footer-secondary">
    <span>© {year} {visible.site.copyright}</span>
    <a href={getRoute(locale, 'privacy')}>{visible.site.privacy}</a>
    <div class="footer-secondary-meta">
      <span>{visible.site.location}</span>
      <a
        class="footer-linkedin"
        href="https://www.linkedin.com/company/clearstance"
        target="_blank"
        rel="noopener noreferrer"
        aria-label={linkedInLabel}
      >
        <svg
          viewBox="0 0 24 24"
          aria-hidden="true"
          focusable="false"
        >
          <path
            fill="currentColor"
            d="M20.45 20.45h-3.56v-5.57c0-1.33-.03-3.04-1.86-3.04-1.86 0-2.14 1.45-2.14 2.94v5.67H9.34V9h3.41v1.56h.05c.47-.9 1.64-1.86 3.37-1.86 3.6 0 4.27 2.37 4.27 5.46v6.29ZM5.34 7.43a2.06 2.06 0 1 1 0-4.12 2.06 2.06 0 0 1 0 4.12Zm1.78 13.02H3.56V9h3.56v11.45ZM22.22 0H1.77C.79 0 0 .77 0 1.72v20.56C0 23.23.79 24 1.77 24h20.45c.98 0 1.78-.77 1.78-1.72V1.72C24 .77 23.2 0 22.22 0Z"
          ></path>
        </svg>
      </a>
    </div>
  </div>
</footer>
